# puente

Traducción offline en tiempo real: **Whisper Tiny INT8** (STT on-device) + **NLLB-200 ONNX** (traducción).

Assets en `puente/assets/models/nllb/` y `puente/assets/models/whisper-tiny/`.

Ver [TESTING.md](puente/TESTING.md) para checks locales y development build. Los scripts `check:nllb` / `check:whisper` cargan modelos grandes en RAM: ejecútalos solo con la máquina desocupada.
